apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "homeboard.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "homeboard.name" . }}
